[project]
name = "naver-places"
version = "0.1.0"
description = "Naver Places MCP server"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/naver_places"]
